#ifndef IO68_DEFAULT_H
#define IO68_DEFAULT_H

enum {
  SPR_DEF = 44100,                      /**< Default sampling rate. */
  SPR_MIN = 8000,                       /**< Minimal sampling rate. */
  SPR_MAX = 96000                       /**< Maximal sampling rate. */
};

#endif
